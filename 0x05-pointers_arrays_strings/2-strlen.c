#include "main.h"
#include <string.h>

/**
 * _strlen - that returns the length of a string
 *
 * @s: name of the string
 * Return: nothing
 */

int _strlen(char *s)
{
	int i = strlen(s);

	return (i);
}
