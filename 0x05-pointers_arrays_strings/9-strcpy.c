#include "main.h"
#include <string.h>

/**
 * _strcpy -  that copies the string pointed to by src to dest
 * @src: the first string
 * @dest: the secound string
 * Return: void
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
		return (dest);
}
